{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "CosmosDb": {
    "Account": "#{CosmosDb_Account}",
    "Key": "#{CosmosDb_Key}",
    "DatabaseName": "#{CosmosDb_DatabaseName}",
    "ContainerName": "#{CosmosDb_ContainerName}"
  },
  "ImageStorage": {
    "ConnectionString": "#{BlobStorage_ConnectionStrin}",
    "ContainerName": "#{BlobStorage_ContainerName}"
  },
  "LogStorage": {
    "Account": "#{TableStorage_Account}",
    "Key": "#{TableStorage_Key}",
    "TableName": "#{TableStorage_TableName}"
  }
}
